# Team Task Manager - Run Project TODO

## Steps
- [x] Install backend dependencies (`cmd /c "cd backend && npm install"`)

- [x] Install frontend dependencies (`cd frontend && npm install`)

- [ ] Start backend server (`cd backend && npm start`)
- [ ] Start frontend server (`cd frontend && npm start`)
- [ ] Verify both servers are running successfully
